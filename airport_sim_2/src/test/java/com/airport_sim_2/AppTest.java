package com.airport_sim_2;

import static org.junit.Assert.assertTrue;
import org.junit.Test;

/**
 * Basic environment check for airport_sim_2.
 * Run 'mvn test' to verify JUnit is working in the new repo.
 */
public class AppTest 
{
    /**
     * If this passes when 'mvn test' is run then Maven environment is set up correctly.
     */
    @Test
    public void shouldAnswerWithTrue()
    {
        assertTrue( true );
    }
}
